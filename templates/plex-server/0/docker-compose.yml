plex:
  image: linuxserver/plex
  volumes:
      - /data/files:/data/files
  links:
    - converter
    - auth
    - html-converter
  environment:
    - VERSION=latest
    - PUID=${PUID}
    - PGID=${PGID}
  ports:
    - ${PORT}:32400
  volumes:
    - ${PATHCONFIG}:/config
    - ${PATHMEDIA}:/data